'use strict';
app.controller('homeController',
    function (
        $scope,
        notificationService) {

        var _initialize = function () {
        };

        $scope.initialize = _initialize;
    }
);